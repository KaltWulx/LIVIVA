package tui

// Context holds global dependencies and state for the TUI
type Context struct {
	// Add global dependencies here (e.g., API client, config)
	// For now, we'll keep it simple as a placeholder
	Width  int
	Height int
}
